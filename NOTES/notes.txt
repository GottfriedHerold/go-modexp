Branches:

RelativeBenchmark:
	- introduces extended benchmarking functionality.
EthereumGasBenchmark:
	- based on RelativeBenchmark
	
Everything else based on EthereumGasBenchmark
(rebase or merge in regularly, changes are to orthogonal files)

Docfixes:
	- minor documentation fixes
	(currently based on EthereumGasBenchmark)


hoist_functions:
	- Montgomery computation in separate function
	- Precomputation in separate function for pow2 case
	(based on docfixed)
	
generalize_alg:
	(based on hoist_function)
	
change_loop_structure
	(based on generalize_alg):
	
	
fix_stack_handling_PrecompPow2:
	Fix broken memory stuff and bugs
	

fix_stack_handling_PrecompMontgomery

improve_inverse_mod2:
	(currently based on EthereumGasBenchmark)
	
improve_mem_Even

improve_mem_pow2

improve_mem_odd


special_case_montgomery
	(currently based on EthereumGasBenchmark)
	
NOTE:
  RelativeBenchmark may need to be reset to correct commit.
